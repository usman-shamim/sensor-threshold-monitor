# Copy this file to ".env" and fill in your real key, OR just export the variable
# in your shell. The ".env" file is git-ignored and must NEVER be committed.
#
# Only needed for the optional --ai diagnosis layer; the tool runs fine without it.

GEMINI_API_KEY=your-gemini-api-key-here
